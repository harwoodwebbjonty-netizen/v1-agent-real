# Project Overview

CoPilotIQ — a sales-team CRM for cold-calling/prospecting UK companies. Two independent parts in one repo: a chat-based WAT (Workflows/Agents/Tools) framework for manual lookups, and a full desktop CRM app (Tauri + FastAPI backend) that a small sales team uses daily.

# Current Architecture

- `app/` — Tauri desktop app (Rust shell + vanilla TypeScript/Vite frontend). No API keys, no direct Anthropic calls — only talks to the backend over HTTP. Builds macOS/Windows installers via `.github/workflows/release.yml`; ships an auto-updater.
- `backend/` — FastAPI service on the VPS (213.165.88.45), served on port 80 via proxy (8000 is firewalled). Owns all Anthropic API calls, SQLite DB (WAL mode), Companies House integration. Deployed manually via rsync (see CLAUDE.md), not by app releases.
- `workflows/*.md` — five of these are read verbatim as **live backend system prompts** (see CLAUDE.md's "Production AI Prompts" table); two others are chat-only SOPs.
- The chat-based WAT layer (`tools/`, `workflows/` SOPs not listed above, `data/phone_lookups.csv`) is separate from the app/backend and used directly in Claude Code sessions.

# Important Components

- `app/src/style.css` — the authoritative design system (tokens + all styling). `CLAUDE.md`'s Design section summarises it; style.css wins on any disagreement.
- `app/src/views/*.ts` — one file per CRM page (actionCentre=Today, dashboard=Leads, coldCallLists, analytics, winBackCampaign, calendar, activityFeed, aiProspecting, settings). **Outreach (v0.6.24 merge)**: `outreach.ts` is now the single front door — one recipient picker (search+checkbox leads, or pick a whole list) drives everything; `emailWriter.ts` (exactly 1 lead) and `listCampaign.ts` (a list, or 2+ hand-picked leads — same backend pipeline either way) are driven by it via exported handles (`focusLead`/`start`) rather than owning their own pickers. `components/outreachDraftGrid.ts` is the shared bulk draft-review UI both `listCampaign.ts` and (later) anything else can reuse. `sequences.ts` is still a separate sub-tab (folding enrollment into the unified picker is the deferred Phase 2 — see Current Work).
- `app/src/components/topBar.ts` — global top-bar wiring (search, bell, user, nav counts).
- `app/src/connection.ts` — the `/health` poll driving both the offline banner and the sidebar footer status dot.
- `backend/app/db.py` — schema + migrations (`CURRENT_SCHEMA_VERSION` + `MIGRATIONS`, append-only, never edit shipped ones).
- `app/src/*Handoff.ts` — tiny shared-module-state handoffs for cross-view "open this specific thing" jumps (`emailWriterHandoff`, `dashboardFilterHandoff`, `opportunityWorkspaceHandoff`, `coldCallListHandoff`). Same pattern each: `setPendingX`/`consumePendingX`, consumed via `subscribeTabs` in the target view.
- `audits/` — point-in-time OS-audit reports checking whether this repo's own docs/memory match reality. Not code; read-only reports.
- `audit/` — distinct from `audits/` above: the 2026-08-12 full product/SaaS-readiness audit (11 docs + reusable scan scripts + JSON results) and its derived `implementation-spec-stage-0.md`. Source of truth for the roadmap currently being executed — see Current Work.
- `backend/app/services/token_crypto.py` — Fernet encryption for OAuth tokens at rest (`TOKEN_ENCRYPTION_KEY`, required setting, no default).

# Data / Database

SQLite at `/opt/v1-agent/backend/data/team.db` on the VPS, WAL mode. Migrations run automatically on backend restart. Companies House filing feed self-prunes to 30 days. Shared lead pool by default — leads outside a list are visible/editable to all users (deliberate).

# Integrations

- Anthropic API (backend only, `backend/.env`)
- Companies House: **two** separate keys — `COMPANIES_HOUSE_API_KEY` (REST, prospecting) and `COMPANIES_HOUSE_STREAM_KEY` (Streaming, live filing feed)
- Mailchimp export (Today page, pending email drafts)
- LinkedIn (chat-tool only, `tools/post_to_linkedin.py` + `linkedin_oauth_setup.py`, credentials via root `.env`)

# Important Project Rules

- `CLAUDE.md` is the single source of truth; `AGENTS.md` is only a pointer to it.
- Any `app/` change needs a version bump in **four** files (`app/package.json`, `app/src-tauri/tauri.conf.json`, `app/src-tauri/Cargo.toml`, the `app` block in `Cargo.lock`) + tag + push — CI fails the build if the tag doesn't match. See CLAUDE.md "Releasing the app."
- Backend deploys are separate (manual rsync) and Claude is blocked from running that command in auto mode — always hand it to the user.
- Editing any of the five production `workflows/*.md` prompts changes real users' live AI calls — see CLAUDE.md's "Production AI Prompts" table before touching any of them.
- `outputs/` and `output/` (two distinct dirs, both hold real client/customer data) must never be committed — both are gitignored as of 2026-08-10.

# Current State

App is at **v0.6.33**; backend is **fully deployed and current as of 2026-08-18** (schema version 39 — every migration through the Outreach merge is live). A multi-release frontend redesign (v0.5.0 → v0.6.4, ~10 releases) moved the whole CRM to an "editorial operations workspace" look matching an approved design artifact — presentation-only, no feature/backend changes. See `CLAUDE.md`'s Design section for the current visual system.

# Current Work

Executing a full product/SaaS-readiness audit's roadmap (`audit/08-prioritised-roadmap.md`), by explicit user decision: **stay single-tenant** (Stage 1/6 skipped) and **no automation builder** (dropped from Stage 5 per explicit user instruction, 2026-08-12). Working through Stage 0 → 2 → 3 → 4 → 5 → 7 autonomously ("keep going automatically till its all done", 2026-08-17), shipping in small verified increments (v0.6.12–v0.6.22 so far).

- **Stage 0 — DEPLOYED and live (2026-08-18)**: OAuth token encryption (`token_crypto.py`, migration 032) and Sentry error tracking (inactive — no `SENTRY_DSN` set, which is fine, it no-ops by design). `TOKEN_ENCRYPTION_KEY` is set in the VPS `.env`. Two ops-only items still need your sign-off: installing `backend/deploy/backup.sh` on cron, and the DB-growth diagnosis query — the latter now has a real lead, see Known Issues.
- **Stage 2 — COMPLETE, including the CSS token pass (v0.6.28–v0.6.33)**: reconnected the 3 orphaned screens, removed dead `data-admin-only` markup + the redundant Settings PIN gate, built a shared modal component (`components/modal.ts`) migrated onto everywhere, fixed activityFeed.ts/aiProspecting.ts conflating "backend unreachable" with "not configured". v0.6.28 added a real skeleton (`skeletonBlockHtml()` in `utils.ts`) to 9 screens that previously showed a blank container or misleading "nothing here" message while loading, and fixed a real bug where `state.ts`/`calendarEvents.ts` never notified subscribers on a failed fetch (added `hasLoadedLeads()`/`hasLoadedEvents()`). **Token pass (v0.6.29–33)**: border-radius got 3 new tokens (`--radius-2xs`/`-xs`/`-full`, zero visual change); font-size got 2 new tokens for genuinely distinct small sizes (`--text-3xs`/`-2xs`) plus the tightly-clustered 0.78–0.9rem mid-range folded into the existing `--text-sm`/`--text-base` (±0.1–0.5px, imperceptible, visually verified across every screen — by explicit user decision after the raw "bypasses the scale" count turned out to mean dozens of sub-pixel-apart values, not real distinct sizes); gap got the same zero-risk exact-match treatment as border-radius. Padding was surveyed and left alone: of 75 hardcoded declarations only one was an exact token match (a coincidental grid-alignment offset, not spacing intent) — the rest are one-off multi-value shorthand tuned per component, not scale drift.
- **Stage 3 — complete**: kanban drag-and-drop board for Opportunities, a real Cmd/Ctrl+K command palette, freeform tags on leads (`lead_tags`, migration 034), saved/shared filter views (v0.6.19: `saved_views`, migration 035, `/saved-views` router; replaced the old local-only, industries-only, `localStorage`-based mechanism in `components/industryFilter.ts`), and custom fields on leads (v0.6.20: genuine key-value, not fixed columns — `custom_field_definitions` + `lead_custom_field_values`, migration 036, `/custom-fields` router admin-gated for create/delete; admin-managed in Settings, editable by any user in the lead side panel).
- **Stage 4 — email + calendar connectors shipped, scoped down by explicit user decision on the rest**: self-service "Connectors" page (`app/src/views/connectors.ts`) with three sections — Email (v0.6.18: Connect Gmail/Microsoft, moved out of admin-only Settings), Calendar (v0.6.21: `calendar_oauth_accounts` table, migration 037, `/calendar-oauth` router, same client id/secret env vars as email OAuth, new scopes `calendar.events`/`Calendars.ReadWrite`; **deliberately one-way (CRM → external) only** — creating a CRM event best-effort-pushes to every connected calendar via `_push_to_connected_calendar` in `calendar.py`, a push failure never fails the local create; inbound pull/two-way sync is explicitly not built, needs real background-job infra), and Calling (informational only: `tel:` fallback, no provider integration — user declined). **One registered OAuth redirect URI for the whole app, self-service, no external console step per integration** (explicit user requirement): calendar OAuth reuses the *already-registered* `/email-oauth/{provider}/callback` URI — migration 038 adds a `purpose` column to `oauth_states` (default `'email'`), `email_oauth.py`'s callback route dispatches to `email_oauth_service` or `calendar_oauth_service` based on it. Any future OAuth connector should follow the same pattern. **The connected Gmail/Outlook account is reserved for Outreach only** — win-back (`winBackCampaign.ts`, `win_back.py`) deliberately never touches it; v0.6.22 removed win-back's direct-send-via-OAuth path entirely, it now sends exclusively through Mailchimp export.
- **Stage 5 — mostly done except automation (dropped)**: 5 missing `lead_id` indexes (migration 033), audit-log coverage gaps closed, pipeline/conversion reporting shipped on Analytics. **Deferred deliberately**: `/leads` pagination — the whole frontend assumes `state.ts` holds every lead at once for client-side filtering, so backend-only params would be unused plumbing without a bigger frontend rearchitecture.
- **Stage 7 — CH engine now scoped, awaiting a go/no-go; the other two items are CLOSED, permanently blocked**: AI call-summary needs a named calling provider — none exists (Communicator/communicator.net has no public API) — out of scope without that dependency. AI-workflow-suggestions needs the automation builder, dropped from scope (2026-08-12) — removed, not deferred. **CH engine finding**: the audit has no concrete spec here, just a "keep investing" flag — deterministic lead-priority scoring from CH data is already built (`lead_scoring_service.py`, migration 026). Real gap found by reading the code: (1) PSC/significant-control data is fetched but never persisted or scored, despite ownership change being a real buying-trigger signal; (2) `ch_service.py` fires concurrent CH requests with zero rate-limit protection — same bug class already fixed once this session for a different module. Scoped options: **A** (half a day) — add the PSC signal; **B** (1-2 days) — A + fix the unthrottled module; **C** (needs budget) — revive the dead Datagardener key for paid financial data. A+B ready to build; C is a business decision.
- **Outreach merge — COMPLETE (v0.6.24–27, separate from the roadmap above — direct user request)**: Email Writer + List Campaign + Sequences enrollment are all now one unified "Compose & Send" screen (`outreach.ts`) driven by a single recipient picker (search+checkbox leads, or pick a whole list) instead of 3 peer tabs — 1 lead shows the rich AI editor, 2+ leads or a whole list shows a shared bulk draft-review grid (`components/outreachDraftGrid.ts`). List campaigns can target either a saved list or an arbitrary hand-picked `lead_ids` set (migration 039) through the same real pipeline. Fixed a pre-existing bug: all 6 "Write Email" entry points were setting a pending-lead handoff that was never consumed. v0.6.25 added manual (non-AI) composition; v0.6.26 added a proper "Email sent" confirmation screen. **v0.6.27 (Phase 2)**: once leads are selected, a send/sequence mode toggle lets a rep enroll them directly via a lightweight sequence-picker overlay (`sequences.ts`'s `enrollLeads()` handle) instead of hunting through a sequence's own dropdown — sequence creation/step-editing still lives only in the Sequences sub-tab.
- **Local dev note**: this Mac's backend `.venv` is Python 3.9, but `schemas_leads.py` uses `X | None` union syntax (3.10+) — any test importing `app.routers.leads` (or anything that transitively imports it) can't collect locally (not new; pre-existing, confirmed via git blame). CI (`backend-tests.yml`) runs 3.11 and is the real verification path for those. `calendar_oauth_service.py`/`calendar.py` don't import leads.py, so `test_calendar_oauth.py`/`test_calendar_events.py` (like `/custom-fields`'s own tests) run locally in full.

# Known Issues

- **RESOLVED: AI email generation 502 in production (2026-08-18).** Was never transient — `generate_email`/`refine_email`'s `EXTRACTION_SCHEMA` had `minimum`/`maximum` on its three `number` fields, which Anthropic's structured-output schema validation rejects outright (400 every call). Only surfaced once swallowed-exception logging was fixed. Fixed by dropping those two keywords (range still communicated via prompt text, not schema-enforced) and deployed. Not yet re-tested live specifically for AI generation (manual compose was used successfully after this deploy, as a separate feature) — worth a final live AI-generation test.
- No Apple notarization (Gatekeeper "damaged app" on new Macs) — needs Apple Developer Program.
- **No HTTPS for the app's main API traffic** — the desktop app's `BACKEND_BASE_URL` (baked in at CI build time) is still `http://213.165.88.45`, plain HTTP to the IP; unrelated to the OAuth fix below and still needs a real domain to fix properly.
- **DB backups exist but only fire on a schema migration** (`db.init_db()`'s automatic pre-migration backup, `BACKUP_RETENTION_COUNT=10`, uncompressed) — never on a plain restart, so real calendar-based coverage is missing between deploys. `backend/deploy/backup.sh` (cron, gzip'd, `KEEP=14`) was written to fill exactly this gap and was never installed — see Current Work / Stage 0.
- **Production DB grew 58MB→1.68GB (~29x) in one month — real lead found (2026-08-18), not yet confirmed.** Deploying migration 033's `CREATE INDEX` on `ch_charge_feed` (the CH filing feed) took several genuinely CPU-bound minutes against the live 1.68GB DB — strongly suggestive that table is the bulk of the growth, despite its documented 30-day self-prune. Next step: `SELECT COUNT(*) FROM ch_charge_feed` on the VPS to confirm, then check whether the prune is actually running/working.
- Win-back emails have no unsubscribe link (blocked on domain/HTTPS).
- **Google + Microsoft OAuth (email connect): RESOLVED and live in production (Google 2026-08-17, Microsoft 2026-08-19).** Google rejects bare-IP redirect URIs — fixed without buying a domain: VPS nginx (`/etc/nginx/sites-enabled/phone-lookup-backend`) serves HTTPS on the free wildcard-DNS hostname `213-165-88-45.sslip.io` via Let's Encrypt (certbot, auto-renews); `PUBLIC_BASE_URL=https://213-165-88-45.sslip.io` + `GOOGLE_OAUTH_CLIENT_ID`/`SECRET` set directly in the VPS `.env`. Microsoft: multi-tenant ("any org directory + personal accounts") Azure AD app registration, `Mail.Send` delegated Graph permission, same shared redirect URI (`/email-oauth/microsoft/callback`); `MICROSOFT_OAUTH_CLIENT_ID`/`SECRET` set in VPS `.env`, backend restarted and confirmed healthy. Verified: Connect Gmail reaches Google's real consent screen; Microsoft app registration + secret in place (end-to-end consent-screen click-through not yet manually verified in the app UI). **Calendars.ReadWrite permission not yet added to the Microsoft app registration** — only `Mail.Send` was granted, so Outlook calendar sync (vs. email) isn't enabled yet. **Watch for on any future certbot run on this file**: it previously moved every `location` block onto the new port-443 block and left port 80 (the app's real plain-IP traffic) 404ing — fixed by duplicating the blocks back onto port 80, redirecting to HTTPS only when `$host` matches the sslip.io hostname. Re-verify `curl http://213.165.88.45/health` after any future certbot edit — not automatic.
- **Activity Feed name/number: RESOLVED and deployed (2026-08-11), verified live.** Two stacked bugs: (1) `COMPANIES_HOUSE_API_KEY` on the VPS was itself invalid — user rotated it, confirmed 200 via direct test. (2) `main.py` was passing a single key into `run_filing_stream()` for both the Streaming connection and the per-event REST follow-ups inside `_process_event()`, which need the separate, non-interchangeable `COMPANIES_HOUSE_API_KEY` — so the REST key was never actually used regardless of validity. Fixed by threading `stream_key`/`rest_api_key` through as distinct params. Deployed; confirmed live: resolved-name count jumped from 30 to 200+ within two minutes of restart, fresh rows showing real names. Also fixes AI Prospecting's CH enrichment, which shares the key path via `activity_refresh_service.py`.
- **New from the above fix, DEPLOYED**: once the charge feed could actually use `COMPANIES_HOUSE_API_KEY`, it started drawing enough volume (several events/sec, up to 2 REST calls each) to exhaust the whole account-wide CH rate limit (~600/5min) and 429 everything else sharing the key. Fixed with a 2s minimum-interval throttle scoped to just the stream's own REST calls (`ch_stream_service.py`), not a global limiter.
- **Backend deploy process gap, fixed 2026-08-18**: the documented deploy command only ever rsynced `backend/app/`, never `backend/requirements.txt` — so a new dependency (`sentry-sdk`) silently never installed on the VPS, crash-looping the service. CLAUDE.md's deploy command now always syncs + `pip install`s requirements.txt (no-op if unchanged). Also: a deploy carrying many queued migrations against a large real DB can legitimately take several minutes, not the usual ~30s — don't restart mid-migration.

# Decisions

- Redesign is presentation-only by explicit instruction: keep every existing feature/button/action; only the visual layer changes.
- `output/` and `outputs/` both stay as separate, gitignored, uncommitted local dirs (not merged/renamed) — their real client data must never enter git history.
- `AGENTS.md` stays a thin pointer, not a duplicate manual — re-collapsed 2026-08-10 after silently reverting once already (see `audits/os-audit-2026-08-10.md`).
- v0.6.5–v0.6.11: worklist/loading-state regression fixes, closed self-registration, identity-panel CSS fix, CI conftest fix, Activity Feed name/number display, win-back FK-constraint 500, Today page worklist improvements, avatar consistency fix, and global session-expiry auto-sign-out. See git log for detail on any of these.
- **2026-08-12: commissioned a full product/SaaS-readiness audit** (`audit/`, 11 docs + scripts) — score 40/100, verdict: mature single-tenant tool, not SaaS-ready (no tenant isolation at all). User decided **stay single-tenant**, skip Stage 1 (multi-tenant foundations) and Stage 6 (commercial launch) — see Current Work for the roadmap now being executed.
- User dropped the automation builder entirely (2026-08-12) — removed from scope, not deferred.
- Shipped **v0.6.12 → v0.6.17** executing the roadmap: Stage 0 backend (token encryption, Sentry), Stage 2 (orphaned-screen reconnection, PIN-gate removal, full modal consolidation, offline-vs-unconfigured error-state fix), Stage 3 (kanban Opportunities board, command palette, freeform lead tags), Stage 5 (missing indexes + audit-log gaps; pipeline/conversion reporting on Analytics) — all now deployed. See Current Work for what's still open in each stage, and commit messages / `audit/implementation-spec-stage-0.md` for full technical detail.
- **v0.6.18**: started Stage 4 but explicitly scoped down after asking — user confirmed calling should stay `tel:`-only (their calling app, "Communicator"/communicator.net, has no public API to integrate against — checked live, no dev docs exist) and wanted email connectors live *now*, not the full connector-framework/named-provider work the roadmap originally scoped. Shipped exactly that slice; the framework/calendar-sync/Aircall items in Stage 4 remain genuinely not started.
- **Win-back email optimisation (backend, deployed)**: prior-email history sent to the writer prompt is no longer capped at 3, and stored lead research no longer expires after 30 days (reused indefinitely instead of re-scraping via Apify). Both mechanisms already existed; this just removed the cap and TTL. No new UI.

# Next Steps

1. **Confirm the DB-growth lead**: `SELECT COUNT(*) FROM ch_charge_feed` on the VPS to check whether that table's self-prune is actually working (see Known Issues). One remaining console step regardless: Google Calendar API / Microsoft `Calendars.ReadWrite` must be enabled as a *permission* on the existing app registration(s) (not a redirect-URI issue, that part's self-service — see Stage 4); Microsoft's registration currently only has `Mail.Send`. Also worth doing: manually click through Connect Microsoft in the app UI to verify the consent screen end-to-end (Google's was; Microsoft's config is in place but unverified live).
2. Install the backup cron (spec'd, needs user sign-off — see Current Work / Stage 0).
3. **CH prospecting engine deepening — awaiting a go/no-go on scope** (see Current Work): PSC-signal + rate-limit-hardening option is scoped and ready to build pending confirmation; a paid-data option needs a separate budget decision.
4. Do a live AI-email-generation test (not just manual compose) to confirm the schema-validation 502 fix actually resolved it in production.
5. Keep this file updated after each meaningful change (see CLAUDE.md's Context Management Rules).
