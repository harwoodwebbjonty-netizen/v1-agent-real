# OS Audit — 2026-08-10

Third run. Compare against `audits/os-audit-2026-07-24.md` (RED baseline) and
`audits/os-audit-2026-07-24-rerun.md` (claimed GREEN fix, same day). Read-only;
this report is the only write.

**Headline: the July 24 fixes never stuck.** Both structural fixes from the
afternoon rerun — collapsing `AGENTS.md` to a pointer, and hardening
`.gitignore` — were verified **on disk** that day but **never committed**.
`git log -- AGENTS.md` and `git log -- .gitignore` show no commits since the
project's first two commits. The working tree was reset at some point after
the rerun (a `git checkout`/`reset`/fresh clone, not investigated further —
out of scope for a read-only audit), silently discarding both fixes. Sixteen
days later, the exact same RED findings are back, plus new ones from the
in-progress CRM redesign (v0.6.0 → v0.6.4, 10 releases since the July 24
baseline).

| Check | Jul 24 AM | Jul 24 PM (claimed) | **Now** | Worst finding |
|---|---|---|---|---|
| Routing integrity | 🔴 RED | 🟢 GREEN | **🔴 RED** | `AGENTS.md` reverted to the full duplicate manual with the pre-rebrand design spec |
| Index truth | 🟡 YELLOW | 🟢 GREEN | 🟡 YELLOW | `MEMORY.md`'s hardening line says app v0.2.14 — reality is v0.6.4 |
| Freshness | 🟡 YELLOW | 🟢 GREEN | **🔴 RED** | `CLAUDE.md`'s own Design section frozen ~10 releases behind the code it's meant to summarise |
| Bloat/duplication | 🟡 YELLOW | 🟢 GREEN | 🟡 YELLOW | `AGENTS.md` duplicate content is back; new near-duplicate `output/`/`outputs/` dirs |
| Hygiene | 🟡 YELLOW | 🟡 YELLOW | **🔴 RED** | `outputs/` (customer-email CSVs) is **not** gitignored despite `CLAUDE.md` claiming it is |
| Context placement | 🔴 RED | 🟢 GREEN | 🟡 YELLOW | `CLAUDE.md` mandates a `PROJECT_CONTEXT.md` that doesn't exist; an open app regression lives only in a memory file |

Secrets remain clean: `.env` and `backend/.env` are both untracked (`git
ls-files` confirms neither is in the index); the three new LinkedIn tool
files (`tools/linkedin_env.py`, `linkedin_oauth_setup.py`,
`post_to_linkedin.py`) all read/write credentials through the same gitignored
root `.env` via a shared `load_env()`/`set_env()` helper — no hardcoded
tokens, no loose credential files. Verified, not assumed.

## Failure-mode exposure

| Mode | Jul 24 AM | Jul 24 PM (claimed) | Now | Driven by |
|---|---|---|---|---|
| Poisoning (false) | HIGH | LOW | **HIGH** | `AGENTS.md` states the pre-rebrand design as current again; `CLAUDE.md`'s own Design section understates 10 releases of change; hardening memory ~30 versions stale |
| Bloat (too much) | MED | LOW | MED | duplicate manual is back; `output/` vs `outputs/` |
| Confusion (wrong/missing) | MED | LOW | MED | `audits/`, `output/`, `WCF_BDE_Weekly_Scorecard_copy.html` unmapped; open regression undocumented anywhere durable |
| Clash (contradictory) | HIGH | LOW | **HIGH** | `CLAUDE.md` vs `AGENTS.md` disagree on design again; `CLAUDE.md` vs `app/src/style.css` (its own declared authority) disagree on font |

## What would make your agent wrong-answer you today

- **"What font/design does the app use?"** `AGENTS.md` says Inter + **Quicksand**, "text colours limited to blue/green/red," "flat surfaces — no gradients, glows, glass, ambient blobs." `CLAUDE.md` says Poppins + Inter, crimson, soft shadows/glows. **Neither is right** — the actual code (`app/src/style.css`, v0.6.2+) uses a single system-sans stack with no Poppins/Inter anywhere, plus the full v0.5–v0.6 editorial redesign (grouped nav, timeline rail, editorial tables). An agent reading either manual first ships the wrong look, and `CLAUDE.md`'s own precedence rule ("style.css is authoritative, this file summarises it") is not enough — a stale summary still misleads unless a reader cross-checks the code every time.
- **"Is it safe to `git add -A` and commit?"** No — `outputs/` (win-back and LinkedIn CSVs, explicitly documented as "may contain customer data — not committed") has **no `.gitignore` entry**. One broad `git add -A` commits customer email addresses to a public-facing GitHub repo. This is the same file both prior audits examined; the July 24 rerun's fix for it was lost.
- **"What's the current app version / has the backend been redeployed?"** `MEMORY.md`'s always-loaded hardening line says v0.2.14; the actual app is **v0.6.4** (10 releases later, all frontend redesign — no backend changes, so no redeploy was needed for these, but nothing in the always-loaded layer says so).
- **"Does the app currently work?"** There's an **open, undiagnosed regression** (user reports empty leads/worklist after the v0.6.3/v0.6.4 update) that exists **only** in a memory file written this session (`project_redesign_artifact.md`) — not in git, not in any tracked doc, not in an issue tracker. If that memory is ever edited or pruned before the bug is fixed, the only record of it disappears.

## Findings by check

### Check 1 — Routing integrity — RED
- **`AGENTS.md` regressed to the exact pre-fix state.** 149 lines, ~95% identical to `CLAUDE.md`, diverging only in the Design block (pre-rebrand: Quicksand, Inter, blue/green/red, flat surfaces, "Codex session" wording) and "Codex"↔"Claude" phrasing. `git log -- AGENTS.md` shows one commit ever (`768e8b7`, the original add) — the July 24 pointer-collapse fix was never committed, so nothing here actually changed; the "GREEN" in the afternoon rerun reflected an uncommitted working-tree edit only. **[clash][poisoning]** — worst finding, repeat of the original.
- **`CLAUDE.md`'s own Design section is stale relative to its declared source of truth.** It documents "body font Inter + display font Poppins 500/600/700," coloured per-section nav icons, and no mention of the grouped-nav / timeline-rail / editorial-table system shipped across v0.5.0–v0.6.4. The actual authoritative file it points to (`app/src/style.css`) now uses a single system-sans stack (no Poppins/Inter for any heading/nav/button) and the full editorial redesign. **[clash][poisoning]**
- **`audits/` still not in `CLAUDE.md`'s directory map**, flagged as trivial in the July 24 rerun, unresolved 17 days later. **[confusion — trivial, but a repeat]**
- **New unmapped `output/` directory** (singular — distinct from the already-known, meant-to-be-gitignored `outputs/`), containing `Tracker.csv` and a `pdf/` folder. Confusable name, not documented anywhere, not gitignored. **[confusion]**
- **`WCF_BDE_Weekly_Scorecard_copy.html`** — a new, content-*different* file alongside the already-documented `WCF_BDE_Weekly_Scorecard.html` ("safe to archive"). Unclear whether this is an accidental duplicate-save or an intentional edit; not documented either way. **[confusion — minor]**
- OK: `n8n/`, `data/`, `outputs/` (as a concept, if not as a gitignore rule), `app/`, `backend/` are all correctly mapped in `CLAUDE.md`'s directory block — that part of the July 24 fix *did* stick (it must have been committed separately from the `AGENTS.md`/`.gitignore` changes). The "Production AI Prompts" table (5 SOPs → call sites) is still accurate against the code. Release-verify repo path still matches the git remote.

### Check 2 — Index truth — YELLOW
- `MEMORY.md` has 7 entries (up from 5 in July), all resolve to real files, no phantoms/orphans. **One entry makes a freshness claim that's now false**: the hardening line states "app now v0.2.14" — actual is **v0.6.4**. The newly-added redesign entry (this session) is accurate as of writing. **[poisoning — one stale claim in the always-loaded layer]**

### Check 3 — Freshness — RED (regressed from GREEN)
| Feed | Raw date | Ingested | Cadence | Verdict | What's missing |
|---|---|---|---|---|---|
| Git / app code | 2026-08-10 | 2026-08-10 | continuous | FRESH | — |
| 5 live workflow SOPs | unchanged since Jul 23 | live-read by backend | on-change | FRESH | — |
| `project_hardening_status` memory | 2026-07-24 | 2026-07-24 | manual | **FROZEN** | says v0.2.14; reality v0.6.4, 10 releases of pure-frontend redesign since |
| `CLAUDE.md` Design section | pre-v0.5.0 | always-loaded | manual | **FROZEN** | entire editorial redesign (grouped nav, timeline rail, system-sans font, footer identity block) not reflected |
| `AGENTS.md` | 2026-07-24 fix, never committed | always-loaded (if opened) | manual | **FROZEN** | reverted to pre-rebrand spec |
| `PROJECT_CONTEXT.md` (mandated by `CLAUDE.md`, uncommitted addition) | never created | n/a | n/a | **MISSING** | the file `CLAUDE.md` says every fresh session should read near the start doesn't exist |

### Check 4 — Bloat/duplication — YELLOW
- `AGENTS.md` duplicate content is back (see Check 1).
- `output/` vs `outputs/` — two similarly-named, differently-purposed directories with no cross-reference.
- `WCF_BDE_Weekly_Scorecard.html` vs `WCF_BDE_Weekly_Scorecard_copy.html` — differing content, purpose of the copy unclear.
- No new manual duplication beyond `AGENTS.md`; `.claude/skills/` contains only unrelated symlinked skills (case-study, client-onboard, dpia-draft, outbound-qa, sow-generator, vps-bootstrap) — none shadow or duplicate this repo's guidance.

### Check 5 — Hygiene — RED (regressed from YELLOW)
- **`outputs/` is not gitignored.** Current `.gitignore` contains only `.env`, `.tmp/`, `kickbacks.vsix`, `.DS_Store`, `__pycache__/`, `*.pyc` — none of the July 24 rerun's claimed additions (`outputs/`, `credentials.json`, `token.json`, `*.inspect.ndjson`) are present. `git log -- .gitignore` confirms only the original two commits ever touched it — same lost-fix pattern as `AGENTS.md`. `outputs/` currently holds `win_back_deals_filterable.csv`, `win_back_ready_ClosedWon_3to6months.csv`, and a LinkedIn company-profiles folder with real prospect emails. **[hygiene — real risk, not cosmetic: one broad `git add` away from committing customer data]**
- **`CLAUDE.md` has a 136-line uncommitted addition** (the "Context Management Rules" section mandating `PROJECT_CONTEXT.md`). Load-bearing instructions sitting only in the working tree are exactly what was lost on July 24 — if this working tree resets again before it's committed, these rules vanish the same way `AGENTS.md`'s fix did.
- **10 uncommitted items**: `CLAUDE.md` (modified), `audits/`, `n8n/`, `output/`, `outputs/`, 3 LinkedIn tool scripts, 2 new `workflows/*.md` (correctly non-production per Check 1), `WCF_BDE_Weekly_Scorecard_copy.html`. This is a pending-decision backlog, not silent drift — but it's grown, not shrunk, since July 24.
- Secrets: clean (see summary above).

### Check 6 — Context placement — YELLOW
- **`PROJECT_CONTEXT.md` is mandated but doesn't exist.** `CLAUDE.md` (in its uncommitted addition) instructs every session to read it "near the start" and keep it updated after "any meaningful task, feature, fix, architectural change" — none of that happened across the 10 redesign releases in this session's history. Either the rule should be enforced going forward, or removed if it's not wanted.
- **The open empty-leads/worklist regression lives only in a memory file**, not in git, not in `PROJECT_CONTEXT.md` (doesn't exist), not in any issue tracker. Correct instinct to capture it before a context-clear, but it's the only durable record right now.
- No situational (point-in-time) facts are baked into `AGENTS.md` (it doesn't carry situational content, just the stale duplicate design spec) — the clash is a manual/code mismatch, not a situational-fact-in-expertise-layer problem this time.

## Fix list

**Batch A — safe, same fix as before (low risk, high value, re-apply and this time commit):**
1. Collapse `AGENTS.md` back to the pointer + precedence line (verbatim as `CLAUDE.md:3` already states the rule) — **and commit it**.
2. Add `outputs/`, `credentials.json`, `token.json`, `*.inspect.ndjson` to `.gitignore` — **and commit it**. This is the highest-priority item (real customer-data exposure risk).
3. Add `audits/` and `output/` to `CLAUDE.md`'s directory map (one line each).

**Batch B — needs your call, not mine:**
4. `CLAUDE.md`'s Design section needs a rewrite to reflect the actual v0.6.4 state (system-sans font, footer identity block, grouped nav, timeline rail, editorial tables) — I can draft it from the current code, but it's a meaningful rewrite of a source-of-truth doc, so flagging rather than doing it silently.
5. The uncommitted "Context Management Rules" / `PROJECT_CONTEXT.md` mandate — **keep it (and I'll create + start maintaining `PROJECT_CONTEXT.md`), edit it, or drop it?** It wasn't written by me this session and I don't know if it reflects a decision you made elsewhere.
6. `project_hardening_status.md` memory — refresh to current v0.6.4 state, or fold into the new redesign memory?
7. `WCF_BDE_Weekly_Scorecard_copy.html` — intentional edit or accidental duplicate? Keep, delete, or rename?
8. The 10 uncommitted items — commit now, or review file-by-file first (per your standing "review before broad commits" preference)?

**Unchanged from before (real-world, not resolvable from here):** DB backup cron, Apple notarization, HTTPS domain — already documented in `CLAUDE.md` "Known gaps."
