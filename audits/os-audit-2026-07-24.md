# OS Audit — 2026-07-24

**Knowledge current through:** the **code / situational layer is live (2026-07-24, app v0.2.14)**, but the **always-consulted expertise layer is frozen at ~v0.1.76 / 2026-07-13** — `AGENTS.md` still describes the pre-rebrand design system, and the hardening memory still lists a backend deploy as "pending." Any agent that reads `AGENTS.md` or trusts the memory index is ~10 versions and ~12 days behind.

| Check | Verdict | Worst finding |
|---|---|---|
| Routing integrity  | **RED**    | `AGENTS.md` ships the wrong design spec; 4 of 5 live production SOPs undocumented |
| Index truth        | YELLOW     | `MEMORY.md` index states v0.1.76 + "backend deploy pending" — stale |
| Freshness          | YELLOW     | `project_hardening_status` memory frozen at v0.1.76 while app is v0.2.14 |
| Bloat/duplication  | YELLOW     | `CLAUDE.md` and `AGENTS.md` are ~95% duplicate manuals, already diverged |
| Hygiene            | YELLOW     | 7 real work items uncommitted (n8n/, outputs/, linkedin tools + workflows) |
| Context placement  | **RED**    | Design spec baked into a second manual with no precedence rule = live clash |

Secrets are clean: `.env` (live Anthropic + Companies House keys) is gitignored **and** untracked; `backend/.env` untracked (only `.env.example` tracked); LinkedIn tools write creds into the gitignored `.env`, not a loose token file. Verified, not assumed.

## Failure-mode exposure

| Mode | Exposure | Driven by |
|---|---|---|
| Poisoning (false)            | **HIGH** | `AGENTS.md` states the old design as current; hardening memory states v0.1.76 + pending deploy as current |
| Bloat (too much)             | MED  | Two near-identical manuals (~300 lines) both in play; `outputs/` mixes scratch with deliverables |
| Confusion (wrong or missing) | MED  | `n8n/` + `outputs/` unmapped; 4 live backend SOPs undocumented; 7 uncommitted items |
| Clash (contradictory)        | **HIGH** | `CLAUDE.md` vs `AGENTS.md` disagree on the design system with no declared winner |

## What would make your agent wrong-answer you today

- **A design change made from `AGENTS.md` would use the wrong brand.** `AGENTS.md` says body font Inter + **Quicksand**, "text colours limited to blue/green/red," and no CoPilotIQ. The app code (`app/src/style.css`) and `CLAUDE.md` say **Poppins**, accent periwinkle **#4F6BFF** + violet #8B5CF6, CoPilotIQ v0.2.0. An agent that opens `AGENTS.md` first ships pre-rebrand styling.
- **"Is the backend deployed / what version are we on?"** The always-loaded `MEMORY.md` line and `project_hardening_status.md` say v0.1.76 with "Deploy backend to VPS" still pending. Reality: app is v0.2.14 and the backend has shipped many times since. The memory also says the backend is at `213.165.88.45:8000`; `CLAUDE.md` says port 8000 is now firewalled and it's served on port 80.
- **"Edit the win-back email prompt / sales-intel prompt — it's just a chat SOP, right?"** No. `workflows/win_back_email_prompt.md`, `find_company_email.md`, `find_person_linkedin.md`, and `sales_intelligence_research.md` are read verbatim as **live production system prompts** by the backend. The manual only documents `find_company_phone_number.md`, so the other four read as "safe to edit" when they're load-bearing.

## Findings by check

### Check 1 — Routing integrity — RED
- **Two operating manuals, one stale.** `AGENTS.md` (149 ln) and `CLAUDE.md` (154 ln) are ~95% identical. They diverge in exactly three spots (`diff`): "Codex"→"Claude" wording (×2), and the entire **Design** block. `AGENTS.md`'s design block is the pre-v0.2.0 spec; `CLAUDE.md`'s is the CoPilotIQ rebrand that matches the code. **[clash][poisoning]** — worst finding.
- **4 of 5 live SOPs undocumented.** Backend reads these as system prompts: `find_company_phone_number.md` (config.py:7), `find_company_email.md` (email_scraper_service.py:17), `find_person_linkedin.md` (linkedin_discovery_service.py:29), `sales_intelligence_research.md` (sales_intelligence_service.py:16), `win_back_email_prompt.md` (win_back_email_service.py:21). The manual documents only the first. **[confusion]**
- **Unmapped active directories:** `n8n/` (lead-autoresponder workflow + README) and `outputs/` (win-back + linkedin CSVs) exist and are referenced by neither manual. **[confusion]**
- **Loose root file** `WCF_BDE_Weekly_Scorecard.html` (Jun 26) — a one-off deliverable at root, unmapped. **[confusion]**
- `.claude/` is an **empty directory** — no project skills/agents. **[cosmetic]**
- `CLAUDE.md`/`AGENTS.md` name `credentials.json, token.json` as "Google OAuth (gitignored)," but `.gitignore` lists neither and they don't exist. Latent: if Google OAuth is ever set up, those files would be tracked. **[hygiene]**
- Illustrative example paths `workflows/scrape_website.md` / `tools/scrape_single_site.py` don't exist — but they're clearly labelled examples. **[cosmetic — ranked last]**
- OK: release-verify repo path `harwoodwebbjonty-netizen/v1-agent-real` matches the git remote; all real routing paths (`backend/README.md`, `backend/app/db.py`, `app/src-tauri/tauri.conf.json` = v0.2.14, `.github/workflows/release.yml`) exist.

### Check 2 — Index truth — YELLOW
- No traditional `_index.md`; the effective index is the manual routing table + `MEMORY.md`. `MEMORY.md`'s two entries both resolve to real files; no phantoms/orphans; the memory dir contains nothing missing from the index. Clean **except**: the `MEMORY.md` hardening line makes a freshness claim ("v0.1.76 shipped… pending: VPS backend deploy…") that is now false. **[poisoning]**

### Check 3 — Freshness — YELLOW

| Feed | Raw date | Ingested date | Cadence | Verdict | What's missing |
|---|---|---|---|---|---|
| Git / app code | 2026-07-24 | 2026-07-24 | continuous | FRESH | — |
| Live workflow SOPs (5) | 2026-07-23 (win-back) | live-read by backend | on-change | FRESH | — |
| `outputs/` (win-back CSVs) | 2026-07-23 | n/a (deliverable) | ON-DEMAND | n/a | per-run export, not a cadence feed |
| `outputs/linkedin-company-profiles/` | 2026-07-17→18 | n/a | ON-DEMAND | n/a | one-off enrichment batch |
| `data/phone_lookups.csv` (118 rows) | 2026-06-26 | n/a | ON-DEMAND | n/a | manual chat-lookup log, ~1 mo idle — idle by design, not broken |
| `project_hardening_status` memory | 2026-07-13 | 2026-07-13 | manual | **FROZEN** | says v0.1.76 + backend-deploy pending; reality is v0.2.14, backend live, port 80 not :8000 |
| `AGENTS.md` design block | pre-v0.2.0 | always-loaded | manual | **FROZEN** | entire CoPilotIQ rebrand |

- `feedback_always_release.md` (23 days old) is timeless guidance and still accurate — not stale (its `v0.1.X` tag is just an example).
- No hooks or crons exist — every "recurring" process (phone log, outputs regen, releases) is manual. Manual-only cadence is why the memory froze. **[stated per Check 5]**

### Check 4 — Bloat, duplication, organization — YELLOW
- **Duplicate manuals** `CLAUDE.md` ≈ `AGENTS.md`, maintained separately and already diverged. Canonical = `CLAUDE.md` (matches code). **[clash][bloat]**
- `outputs/` mixes deliverables (`.csv`, `.xlsx`) with scratch (`*.inspect.ndjson`, `.DS_Store`) at repo root. WAT rule: intermediates → `.tmp/`, deliverables → cloud. `outputs/` is an unmapped in-between. **[bloat][confusion]**
- Root hygiene: `WCF_BDE_Weekly_Scorecard.html`, `outputs/`, `n8n/` all sit at root; root should read like a table of contents.
- 13 `.DS_Store` files scattered (gitignored — cosmetic). **[cosmetic]**
- `.tmp/` is 156K incl. `node_modules` — fine; it's the gitignored disposable zone with a `.gitkeep`. Not a finding.
- Always-loaded weight: ~300 lines across two near-identical manuals; collapsing to one halves it. **[bloat]**

### Check 5 — Hygiene and silent failures — YELLOW
- **Secrets: clean (verified).** `.env` gitignored + untracked; `backend/.env` untracked (`.env.example` only); no `token.json`/`credentials.json` on disk; LinkedIn tools persist creds to the gitignored `.env` via `tools/linkedin_env.py`.
- **7 uncommitted work items:** `n8n/`, `outputs/`, `tools/linkedin_env.py`, `tools/linkedin_oauth_setup.py`, `tools/post_to_linkedin.py`, `workflows/linkedin_post.md`, `workflows/win_back_ui_process.md` — all untracked. Real tooling/SOPs with no git history or rollback, which cuts against your own standing "always release/commit after changes" rule. **[confusion / durability]**
- `.gitignore` gap: doesn't cover the `credentials.json`/`token.json` the manual claims are ignored (latent).
- Dead capabilities: none loadable — `.claude/` is empty (nothing to silently fail).

### Check 6 — Context placement — RED
- **Situational baked into expertise (×2):** (a) `AGENTS.md`'s concrete design spec sits in an always-consulted manual — it went stale and now actively misinforms; (b) the `MEMORY.md`/`project_hardening_status` "v0.1.76 + pending deploy" snapshot sits in the always-loaded index. Each is a double hit: guaranteed poisoning + per-session bloat. Fix = pointer, not a fresher copy. **[poisoning][bloat]**
- **No precedence rule.** Nothing states that `CLAUDE.md` wins over `AGENTS.md`, so every duplicated fact is a clash waiting to fire — and one already has (design). **[clash]**
- **Expertise buried in situational store:** "these 5 workflow files are live production prompts" is discoverable only by reading backend code, not from the manual. Promote it to `CLAUDE.md`. **[confusion]**

## Questions for you

1. **`AGENTS.md` — keep it or delete it?** It's a stale twin of `CLAUDE.md` from the Codex era. Recommended: delete it (or replace its body with a one-line pointer to `CLAUDE.md`) so there's a single source of truth. Confirm before I touch it.
2. **`data/phone_lookups.csv` idle since Jun 26 — did you stop manual chat-based phone lookups on purpose** (i.e. the desktop app fully replaced them)? If yes, the fix is a label, not a revival.
3. **`outputs/` and `n8n/` — commit them or keep them local/untracked?** They're real but outside git. Tell me which belong in version control vs. `.tmp/`/cloud.
4. **Nightly DB backup cron** for `/opt/v1-agent/backend/data/team.db` — the memory still lists this as unresolved. Did it get set up?

## Fix list (batched, await approval)

**Batch A — security + dead capabilities (do first)**
- Add `credentials.json` and `token.json` to `.gitignore` (latent Google-OAuth exposure).
- Note: no live secret exposure found — this is preventive.

**Batch B — routing + index reconciliation (biggest correctness win)**
- Resolve the two-manual clash: make `CLAUDE.md` canonical; delete `AGENTS.md` or reduce it to a pointer (pending your answer to Q1).
- In `CLAUDE.md`: document that the backend live-reads **five** workflow SOPs as system prompts (list them), and add `n8n/` + `outputs/` to the directory map. Re-home or map `WCF_BDE_Weekly_Scorecard.html`.
- Remove the empty `.claude/` dir or populate it intentionally.

**Batch C — data / memory catch-up**
- Update `project_hardening_status.md` + its `MEMORY.md` line to current reality (app v0.2.14, backend deployed, port 80 not :8000), or convert it to a pointer to `CLAUDE.md`'s "Known gaps" so status lives in exactly one place.
- Label `data/phone_lookups.csv` as the manual-lookup log (snapshot, superseded by the app) per Q2.
- Commit or relocate the 7 untracked items per Q3.

**Batch D — durability (so it can't silently freeze again)**
- Establish a nightly `team.db` backup cron (Q4) and record it in `CLAUDE.md`.
- Consider a single precedence line in `CLAUDE.md`: "`CLAUDE.md` is the source of truth; the app code is the source of truth for the design system — the manual points at it." Prevents the next design-spec drift.
