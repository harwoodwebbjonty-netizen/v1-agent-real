# Agent Instructions

> This file is a pointer. `CLAUDE.md` in this same directory is the single source of truth for how to work in this repo — read that instead. If you're an agent that only reads `AGENTS.md` by convention, treat this line as your redirect: go read `CLAUDE.md`.
